Changes in component 'nf-core/plink2/score'
'modules/nf-core/plink2/score/environment.yml' is unchanged
'modules/nf-core/plink2/score/meta.yml' is unchanged
Changes in 'plink2/score/main.nf':
--- modules/nf-core/plink2/score/main.nf
+++ modules/nf-core/plink2/score/main.nf
@@ -1,34 +1,35 @@
 process PLINK2_SCORE {
-    tag "$meta.id"
-    label 'process_low'
+	tag "${meta.id}"
+	label 'process_low'
 
-    conda "${moduleDir}/environment.yml"
-    container "${ workflow.containerEngine == 'singularity' && !task.ext.singularity_pull_docker_container ?
-        'https://depot.galaxyproject.org/singularity/plink2:2.00a2.3--h712d239_1' :
-        'biocontainers/plink2:2.00a2.3--h712d239_1' }"
+	conda "${moduleDir}/environment.yml"
+	container "${workflow.containerEngine == 'singularity' && !task.ext.singularity_pull_docker_container
+		? 'https://depot.galaxyproject.org/singularity/plink2:2.00a2.3--h712d239_1'
+		: 'biocontainers/plink2:2.00a2.3--h712d239_1'}"
 
-    input:
-    tuple val(meta), path(pgen), path(psam), path(pvar)
-    path(scorefile)
+	input:
+	tuple val(meta), path(pgen), path(psam), path(pvar), path(afreq), path(scorefile)
 
-    output:
-    tuple val(meta), path("*.sscore"), emit: score
-    path("versions.yml")             , emit: versions
+	output:
+	tuple val(meta), path("*.sscore"), emit: score
+	path ("versions.yml"), emit: versions
 
-    when:
-    task.ext.when == null || task.ext.when
+	when:
+	task.ext.when == null || task.ext.when
 
-    script:
-    def args = task.ext.args ?: ''
-    def prefix = task.ext.prefix ?: "${meta.id}"
-    def mem_mb = task.memory.toMega() // plink is greedy
-    """
+	script:
+	def args = task.ext.args ?: ''
+	def prefix = task.ext.prefix ?: "${meta.id}"
+	def mem_mb = task.memory.toMega()
+	// plink is greedy
+	"""
     plink2 \\
-        --threads $task.cpus \\
-        --memory $mem_mb \\
+        --threads ${task.cpus} \\
+        --memory ${mem_mb} \\
         --pfile ${pgen.baseName} \\
-        --score ${scorefile} \\
-        $args \\
+        --score ${scorefile} header cols=+scoresums,+denom,+fid no-mean-imputation \\
+        --read-freq ${afreq} \\
+        ${args} \\
         --out ${prefix}
 
     cat <<-END_VERSIONS > versions.yml
@@ -36,4 +37,16 @@
         plink2: \$(plink2 --version 2>&1 | sed 's/^PLINK v//; s/ 64.*\$//' )
     END_VERSIONS
     """
+
+
+	stub:
+	def prefix = task.ext.prefix ?: "${meta.id}"
+	"""
+	touch ${prefix}.sscore
+
+	cat <<-END_VERSIONS > versions.yml
+	"${task.process}":
+        plink2: \$(plink2 --version 2>&1 | sed 's/^PLINK v//; s/ 64.*\$//' )
+	END_VERSIONS
+	"""
 }

'modules/nf-core/plink2/score/tests/main.nf.test.snap' is unchanged
'modules/nf-core/plink2/score/tests/nextflow.config' is unchanged
'modules/nf-core/plink2/score/tests/main.nf.test' is unchanged
************************************************************
